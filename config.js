// Config Supabase — à remplir une fois le projet Supabase créé (voir README).
// L'anon key est publique par design (la sécurité vient des règles RLS + liste d'emails autorisés).
window.JUJU_CONFIG = {
  SUPABASE_URL: "",      // ex. "https://xxxx.supabase.co"
  SUPABASE_ANON_KEY: ""  // la clé "anon public" du projet
};
